services:
  web:
    build: .
    ports:
      - "8018:8018"
    volumes:
      - ./media:/app/media
      - ./db.sqlite3:/app/db.sqlite3
    environment:
      - DEBUG=False
      - SECRET_KEY=your-secret-key-change-in-production
    restart: unless-stopped
